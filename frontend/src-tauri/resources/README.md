# Bundled resources

Optional payloads for the Tauri bundle.

- Put offline Ollama models under `resources/ollama_models/` if you choose to ship them inside the installer.

If you do not bundle models, this folder can stay empty.
